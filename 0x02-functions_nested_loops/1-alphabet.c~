#include "main.h"
/**
 * main - Prints a random number and states whether
 * it is positive, negative, or zero.
 * 
 * 
 * Return: Always 0.
 */
int main(void)
{
int ch;
for (ch = 0; ch < 8; ch++)
_putchar(str[ch]);
_putchar('\n');
return (0);
}
